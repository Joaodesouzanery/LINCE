# Dependências de runtime do módulo Financeiro
# (o módulo é deliberadamente magro — sem lib de PDF, planilha, data ou moeda)

@supabase/supabase-js  ^2.45.0    # cliente do banco + Storage (upload/remove/signed URL)

# --- NÃO são necessárias ---
# lib de PDF       : a impressão usa window.print() do próprio navegador
# lib de moeda     : Intl.NumberFormat resolve
# lib de data      : só strings YYYY-MM-DD e toLocaleDateString
# valor por extenso: implementado em ~30 linhas no próprio módulo
