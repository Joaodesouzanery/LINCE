# Dependências do módulo Eventos

## Backend
@supabase/supabase-js  ^2.45.0    # banco (o módulo não usa Storage)

## Frontend
SheetJS (xlsx)  0.20.3            # import/export de planilha — VENDORIZADO, ver nota

# --- NOTA SOBRE O SHEETJS ---
# NÃO instale pelo npm/jsDelivr: a distribuição publicada lá parou numa release antiga
# com CVE de prototype pollution. O dist oficial vem de https://cdn.sheetjs.com e no
# projeto de origem está vendorizado no repo (vendor/xlsx.full.min.js, ~950 KB),
# carregado por <script> — o que também pinna a versão.
# Só é necessário se você quiser import/export de planilha; o resto do módulo não usa.

## Opcional (só para o Score de Patrocinador, que NÃO vem neste pacote)
# ANTHROPIC_API_KEY — geração do ângulo comercial por IA
